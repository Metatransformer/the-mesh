# === Interchange (optional) ===
# Point to a remote interchange server. If unset, `pnpm dev` runs one locally.
# INTERCHANGE_URL=https://your-mesh.example.com
# INTERCHANGE_PORT=3001

# === Federation (optional) ===
# Connect this mesh instance to a gateway for cross-mesh communication.
# Leave unset to run standalone (no federation).

# Your unique mesh instance ID
# MESH_ID=my-mesh

# Shared secret (must match what you registered at the gateway)
# MESH_SECRET=change-me-to-something-strong

# Gateway WebSocket URL — point to your own gateway
# MESH_GATEWAY_URL=ws://localhost:4000/gateway
